{
  "name": "multi-agent-real-estate-intelligence-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ui && npm install && npm run build && cd .. && rm -rf public && cp -r ui/dist public"
  }
}
